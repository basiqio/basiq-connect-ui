{
  "name": "basiq-blink-js-control",
  "version": "0.5.0",
  "description": "Basiq drop in control for js clients",
  "main": "app.js",
  "scripts": {
    "build": "./build/read-environment.sh > config.json && webpack",
    "deploy": "build/deploy.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Basiq.io",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^4.31.0",
    "webpack-cli": "^3.3.2"
  }
}
